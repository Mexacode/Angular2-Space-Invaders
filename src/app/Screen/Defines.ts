export class Defines  {
    static ALLY_RATIO = 0.10; 
    static ENEMY_RATIO = 0.09;
    static BULLET_RATIO = 0.6;     
   
}

export enum Animations {
    ANIMATIONS_EXPLOTION = 0
}

export enum ActorState {
    IDDLE_STATE = 0,
    UP_STATE ,
    DOWN_STATE,
    LEFT_STATE,
    RIGTH_STATE,
    EXPLOTION_STATE,
    DELETED_STATE,    
}